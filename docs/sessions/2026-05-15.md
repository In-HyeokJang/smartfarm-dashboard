# Session: 2026-05-15

## 작업 내용

### 펌프 수동 제어 버그 수정 (`D:\jay\workspace\arduino\smartfarm\smartfarm.ino`)
- **버그**: 대시보드에서 "펌프 OFF" 버튼을 눌러도 3초마다 자동 제어가 덮어써서 다시 ON으로 변하는 문제
- **원인**: `loop()`의 자동 제어 조건(`soilRaw >= DRY_THRESHOLD`)이 수동 명령보다 우선 실행됨
  - 센서가 흙에 꽂혀있지 않으면 `soilRaw = 1022~1023` → 항상 "건조" 판정 → 3초마다 `pumpOn()` 강제 실행
- **수정**: 수동 모드 플래그(`manualMode`) 추가
  - `PUMP_ON` / `PUMP_OFF` 명령 수신 시 → `manualMode = true`, 30초 타이머 시작
  - 자동 제어 블록을 `if (!manualMode)` 로 감쌈
  - 30초 후 `manualMode = false` → 자동 제어 재개

## 다음 세션 참고사항
- Arduino IDE에서 수정된 스케치 업로드 필요 (아직 업로드 여부 미확인)
- 센서를 흙에 꽂아야 정상 수치(0~1023 범위 내 중간값) 나옴 — 꽂혀있지 않으면 1022~1023
- Arduino IDE 시리얼 모니터 열려 있으면 COM3 점유 → Spring Boot 연결 실패하니 반드시 닫을 것